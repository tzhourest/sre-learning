# 学习笔记
日期：2026-05-02
主题：输出重定向和管道介绍

## 核心命令
- '> file' 输出到文件里并且覆盖文件里的内容
- '>> file' 输出到文件里并且追加在之前的内容的最后
- '2 > file' 错误的输出也能进入到文件里
- '2 > /dev/null' 错误的输出丢掉
- '> file 2 > &1','& > file' 无论错误还是正确的输出都进入文件

##  注意事项
- '>'命令比较危险，因为会覆盖之前的内容
- 错误的命令不能进入|，如果想要捕获需要配合'2 > &1'
